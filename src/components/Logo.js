import React from 'react';

export default function Logo() {
    return (
        <img
            style={{
                display: 'block',
                maxWidth: 230,
                maxHeight: 75,
                width: 'auto',
                height: 'auto',
            }}
            alt=""
            src="logo.png"
        ></img>
    );
}
